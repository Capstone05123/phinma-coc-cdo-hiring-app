import 'package:flutter/material.dart';

class GlobalSplashScreen extends StatelessWidget {
  const GlobalSplashScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
